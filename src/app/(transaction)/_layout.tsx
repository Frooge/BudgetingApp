import { Stack } from "expo-router";

export default function TrabsactionLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
